-- Enable required extensions
create extension if not exists "uuid-ossp" schema extensions;
-- pgvector will be enabled in Sesión 4 (RAG embeddings)
-- create extension if not exists "pgvector" schema extensions;
